#!/usr/bin/env bashio
networks=$(bashio::config 'networks')
tftproot=$(bashio::config 'tftproot')
pidfile=/tmp/tftp-pidfile.pid

echo "Starting tftpd..."
/usr/sbin/in.tftpd --verbose --foreground --verbose --secure --pidfile ${pidfile} --address :69 ${pidfile}

# wait "$(cat ${pidfile})"
